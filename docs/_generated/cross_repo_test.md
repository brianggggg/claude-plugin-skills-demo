Pulled live from the sibling `TestGhostProject` checkout: **10 tracked items**. Combined with this catalog's **8 skills**, that's **18** items across both repos.

**Letter matches** — skill name and other-repo file share a first letter:

- `invoice-extractor` ↔ `index.html` (both start with "i")
- `meeting-summarizer` ↔ `mkdocs.yml` (both start with "m")
- `weekly-report-writer` ↔ `wood.png` (both start with "w")

---
description: Review a batch of expense line items against policy and flag exceptions
---

Audit the expense report(s) in `$ARGUMENTS` or attached to this conversation.

1. Use the `expense-policy-checker` skill against every line item.
2. Group results into "clean", "needs receipt", and "policy violation".
3. Summarize total spend and the dollar amount under review in each group.
